% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_tests.R
\name{bootstrap_one_way}
\alias{bootstrap_one_way}
\title{Test the equality of any parameter of unknown distributions via bias corrected accelerated bootstrapping}
\source{
\itemize{
\item \url{https://en.wikipedia.org/wiki/Likelihood-ratio_test}
\item Yudi Pawitan. In All Likelihood. Oxford University Press.
\item Hodd, McKean, and Craig. Introduction to Mathematical Statistics. Pearson.
\item Efron, Tibshirani. An introduction to the Bootstrap. Chapman & Hall/CRC.
}
}
\usage{
bootstrap_one_way(x, fctr, stat.fun, ci.fun, conf.level = 0.95)
}
\arguments{
\item{x}{a numeric vector.}

\item{fctr}{a factor vector indicating groups.}

\item{stat.fun}{a function to compute observed statistic.}

\item{ci.fun}{a function to compute confidence intervel.}

\item{conf.level}{overall confidence level of the likelihood intervals. Uses Bonferroni correction.}
}
\value{
An S3 class containing the test statistic, p value, list of likelihood based confidence intervals,
overall confidence level, individual confidence level of each interval and alternative
hypothesis.
}
\description{
Test the equality of any parameter of unknown distributions via bias corrected accelerated bootstrapping
}
\examples{
library(LRTesteR)

# Null is true
stat.fun <- function(x, fctr) {
  groupMeans <- by(x, fctr, mean)
  stat <- max(groupMeans) - min(groupMeans)

  return(stat)
}

ci.fun <- function(x) {
  out <- mean(x)
  return(out)
}

set.seed(1)
x <- rnorm(30, 1, 1)
fctr <- c(rep(1, 10), rep(2, 10), rep(3, 10))
fctr <- factor(fctr, levels = c("1", "2", "3"))
bootstrap_one_way(x, fctr, stat.fun, ci.fun)

# Null is false
set.seed(1)
x <- c(rnorm(10, 1, 1), rnorm(10, 2, 1), rnorm(10))
fctr <- c(rep(1, 10), rep(2, 10), rep(3, 10))
fctr <- factor(fctr, levels = c("1", "2", "3"))
bootstrap_one_way(x, fctr, stat.fun, ci.fun)

}
